version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      # 可按需传递给 Vite 的构建环境变量（示例）
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-}
